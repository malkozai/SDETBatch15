package class2;

public class DecimalNumber {

    public static void main(String[] args) {
        /*
        if we want to store decimal numbers 10.5 110.2 11.4 in computer's memory using java we can use
        following types of boxes.
        1) float(smaller number to store is called float) 2) decimal
         */
        float myNumberBox= 10.5f;
        float MySecondBox=5.5f;
        double largeDecimalBox=13456544.121212;
        System.out.println(myNumberBox);

        /*
        to store letters/special characters/symbols we use char and maximum number to put
        the letters is only 1 letter or character or symbols and with the char datatype we only use
        single quotes
         */
        char letter='k';
        char gender='M';
        char symbol='#';
        char numberChar='1';
        /*
        String data type belongs to a different category called non-primitives
        that is why for string rules are a little different for example S of string is upper case
        its color is also not blue
        we use string if we have more than one character below is example
         */

        String name="Sam";

        /*
        if you only want to store to value such as  yes (True) or no (Fals) comand
        we use boolean datatype
         */
        boolean hungry=true;    // true means yes
        boolean sleepy=false;   // false means no





    }
}
